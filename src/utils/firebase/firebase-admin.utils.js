
import * as admin from "firebase-admin";

const adminConfig = {
  // credential: applicationDefault(),
  // databaseURL: "https://vitecrown.firebaseio.com"
};

// const adminApp = initializeApp(adminConfig);

const adminApp = ''

export default adminApp;
